using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MakeStill : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Left: 14 these are triggers
        //Right: 15
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
